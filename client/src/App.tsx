import React from "react";

export default function App() {
  return (
    <div style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>AgenteIA CSV — App funcionando</h1>
      <p>Esta es una aplicación mínima creada para lanzar con Vite.</p>
      <p>
        Ejecuta <code>npm run dev:client</code> y abre el puerto indicado.
      </p>
    </div>
  );
}
